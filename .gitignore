node_modules/
dist/
# Node bundle the prerenderer imports during the build. Regenerated by
# `npm run build:ssr`, never served.
dist-ssr/
!docs/**/dist/
!docs/**/dist/*.html
!docs/**/dist/*.md
!docs/**/dist/*.pdf
docs/**/dist/.swift-cache/
.env
*.db
*.db-journal
data/*.json
tmp/
.DS_Store

# Git worktrees checked out inside the repo. These are working copies of the
# same history, not content — committing one would nest the repo in itself.
worktrees/

# Local Netlify folder
.netlify

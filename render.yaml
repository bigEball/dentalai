services:
  - type: web
    name: smart-dental-ai
    runtime: node
    buildCommand: npm run build
    startCommand: npm start
    disk:
      name: smart-dental-ai-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: file:../data/dentalai.db
